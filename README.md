# Cat Food Detector
ตรวจจับอาหารแมวจากกล้องแบบเรียลไทม์ โดยประเมิน `% ปริมาณในชาม` จากพื้นที่อาหารที่พบในภาชนะ

## ติดตั้ง

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```


```bash
python3 -m app
```


```text
http://127.0.0.1:3000
```
